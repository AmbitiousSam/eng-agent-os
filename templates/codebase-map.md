# Repo Map — <repo name>

> Built at git SHA: <sha> · last refreshed: <date>. Cached in `.eaos/memory/codebase/`.

## Stack
- Languages / runtimes: <…>
- Frameworks / key libs: <…>
- Package manager(s): <…>

## Commands (verified)
- build: `<cmd>`
- run / dev: `<cmd>`
- test: `<cmd>`   (single test: `<cmd>`)
- lint / format: `<cmd>`
- migrations: `<cmd>`

## Structure (responsibilities)
| Path | Responsibility |
|---|---|
| `src/…` | <one line> |

## Entry points
- <e.g. src/server.ts — HTTP server & route registration>

## Conventions
- Naming: <…> · Errors: <…> · Logging: <…> · Layering: <…>
- Tests: <framework, location, style>

## Key modules (public interface)
- `<module>` → <what depends on it, its main API>

## Integrations
- <DB, queues, external APIs, auth provider…>

## Danger zones (pull specialists / extra review)
- <auth / migrations / payments / generated code / public API / perf-critical paths>
