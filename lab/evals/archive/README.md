# evals/archive

`v3-routing-golden.yaml` — the 16-case routing fixture for EAOS v3 (playbook selection and
minimum rosters). v4 has no roster or playbooks; the model routes. Kept for the `e0-baseline`
tag and for E0, which runs against v3. `scripts/eval_check.py` skips when no live fixture exists.
